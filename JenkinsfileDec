pipeline {
    agent any
    def wxx =  "sh returnStdout: true, script: 'echo whoami'"
    
    stages {
        stage ('Compile Stage') {
            steps {
                echo  'Compile Stage start'
                echo  "${PATH}"
                echo "${wxx}"
                echo  'Compile Stage end'
            }
        }

        stage ('Testing Stage') {
            steps {
                echo  'mvn test'
            }
        }

        stage ('installing Stage') {
            steps {
                    echo  'mvn install'
            }
        }

        stage ('Deployment Stage') {
            steps {
                echo  'mvn deploy'
            }
        }
    }
}
